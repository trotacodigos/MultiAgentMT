version: "v1.0"

reproducibility:
  seed: 42

task: postedit

# Skip processing if translation is already provided in the 'target' column
# Applicable for postedit and proofread tasks
skip_translate_if_provided: false

model:
  name: gpt-4.1-mini-2025-04-14
  temperature: 0.7
  max_tokens: 2048

prompt:
  version: "1.0"
